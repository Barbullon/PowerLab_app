Pyinstaller:

pip install auto-py-to-exe


In auto-py-to-exe
add folder: app_files, ahrs,customtkinter

in app change:

def path_join(name):
    path_name = os.path.join(os.path.dirname(__file__), name)
    return path_name

path_join('app_files\icon.png')

iconbitmap(sys.executable) ok

	globa Data

    orig_stdout = sys.stdout
    f = open('RunDetails.txt', 'w')
    sys.stdout = f

	if
	elif
	else:

    sys.stdout = orig_stdout
    f.close()

Accelerometer.csv,Acceleration x (m/s^2),Acceleration y (m/s^2),Acceleration z (m/s^2),time
Gyroscope.csv,Gyroscope x (rad/s),Gyroscope y (rad/s),Gyroscope z (rad/s),time